# Parity Harness (Bootstrap)

This directory contains the strict-ABI vs wrapper parity harness scaffold.

Initial scope:
- canonical vector loader
- strict ABI runner stub
- result comparator skeleton
